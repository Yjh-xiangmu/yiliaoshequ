<!--pages/pending-orders/pending-orders.wxml-->
<view class="page">
  <!-- 顶部统计 -->
  <view class="stats-bar">
    <view class="stats-item">
      <view class="stats-value">{{totalCount}}</view>
      <view class="stats-label">待接订单</view>
    </view>
  </view>

  <!-- 订单列表 -->
  <view class="order-list">
    <!-- 加载中 -->
    <view wx:if="{{loading}}" class="loading">
      <text>加载中...</text>
    </view>

    <!-- 订单卡片 -->
    <view 
      wx:for="{{orderList}}" 
      wx:key="id" 
      class="order-card"
    >
      <!-- 订单头部 -->
      <view class="order-header">
        <view class="order-no">订单号：{{item.orderNo}}</view>
        <view class="order-status pending">待接单</view>
      </view>

      <!-- 医院信息 -->
      <view class="hospital-info">
        <view class="hospital-name">
          <text class="icon">🏥</text>
          <text class="name">{{item.hospitalName}}</text>
        </view>
        <view class="department">{{item.department}}</view>
      </view>

      <!-- 就诊信息 -->
      <view class="patient-info">
        <view class="info-row">
          <view class="label">就诊人：</view>
          <view class="value">{{item.patientName}}</view>
        </view>
        <view class="info-row">
          <view class="label">联系电话：</view>
          <view class="value">{{item.patientPhone}}</view>
        </view>
        <view class="info-row">
          <view class="label">预约时间：</view>
          <view class="value">{{item.appointmentTime}}</view>
        </view>
      </view>

      <!-- 服务信息 -->
      <view class="service-info">
        <view class="service-type">
          <text class="icon">📋</text>
          <text class="text">{{item.serviceType}}</text>
        </view>
        <view class="price">¥{{item.price}}</view>
      </view>

      <!-- 特殊需求 -->
      <view wx:if="{{item.specialRequirements}}" class="special-requirements">
        <view class="label">特殊需求：</view>
        <view class="content">{{item.specialRequirements}}</view>
      </view>

      <!-- 操作按钮 -->
      <view class="order-actions">
        <view class="action-btn secondary" bindtap="viewDetail" data-id="{{item.id}}">
          查看详情
        </view>
        <view class="action-btn primary" bindtap="acceptOrder" data-id="{{item.id}}" data-no="{{item.orderNo}}">
          立即接单
        </view>
      </view>
    </view>

    <!-- 空状态 -->
    <view wx:if="{{!loading && orderList.length === 0}}" class="empty">
      <text class="empty-icon">📋</text>
      <text class="empty-text">暂无待接订单</text>
    </view>
  </view>
</view>